- tool: TypeSpecValidation
  paths:
    - tspconfig.yaml
  rules:
    - SdkTspConfigValidation
  reason: "This would be added later in the process, but we want to ensure that the configuration is valid."