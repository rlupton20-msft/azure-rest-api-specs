- tool: TypeSpecValidation
  paths:
    # Specify the path to the tspconfig.yaml file where suppression should apply
    - IotFirmwareDefense.Management/tspconfig.yaml
  rules:
    - SdkTspConfigValidation
  reason: SDK is defined in OpenAPI file, not in TypeSpec
- tool: Swagger Avocado
  paths:
    - Private.IoTFirmwareDefense/**/*.json
  reason: Avocado is failing for some reason now on this old private RP specification
