- tool: TypeSpecValidation
  paths:
    - Extension.Management/tspconfig.yaml
    - Dashboard.Management/tspconfig.yaml
  rules:
    - SdkTspConfigValidation
  reason: These two resources are not related to tenant settings and should not be validated by tenant settings-specific rules.