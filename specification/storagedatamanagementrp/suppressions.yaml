- tool: TypeSpecValidation
  paths:
    # Specify the path to the tspconfig.yaml file where suppression should apply
    - Private.StorageDataManagement.Management/tspconfig.yaml
  rules:
    - SdkTspConfigValidation
  reason: No need to generate SDKs # Explain why this suppression is necessary